# Docker Hub Authentication (optional but recommended for higher rate limits)
DOCKERHUB_USERNAME=your_username
DOCKERHUB_PASSWORD=your_password

# Registry URLs (optional, defaults provided)
MCP_REGISTRY_URL=https://registry.modelcontextprotocol.org
DOCKERHUB_BASE_URL=https://hub.docker.com

# API Configuration
DEFAULT_PAGE_SIZE=100
MAX_PAGE_SIZE=1000

# Web Server Configuration
WEB_HOST=0.0.0.0
WEB_PORT=8000
DEBUG=false

GITHUB_TOKEN=your_github_token_here